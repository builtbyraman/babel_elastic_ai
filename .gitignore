node_modules
.DS_Store

# Secrets / local config
.env
# .mcp.json — local MCP registration (contains credentials)
.mcp.json

# Python — virtualenvs, bytecode caches
.venv/
__pycache__/
*.pyc

# Runtime logs (MCP audit log, sigma converter log, etc.)
*.log

# Build artifacts — regenerated by "npm run build".
# Ignore everything under target/ except the distributable zip.
target/*
!target/babel-*.zip
